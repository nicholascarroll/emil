.Dd February 10, 2026
.Dt EMIL 1
.Os
.Sh 名称
.Nm emil
.Nd 文本编辑器
.Sh 概要
.Nm
.Op Ar 选项
.Op Ar 文件
.Sh 描述
.Nm
是一款小巧、可移植的 UTF-8 编码文本编辑器。
它专为在 VT100 兼容终端上运行而设计，并实现了
.Em Emacs
编辑器操作惯例的一个子集。
.Pp
本手册页是
.Nm
.Sh 选项
.Bl -tag -width Ds
.It +N
打开文件时将光标定位到第 N 行。
.It --version
显示版本信息。
.El
.Sh 核心概念
.Nm
遵循 Emacs 风格的范式。理解这些概念对于有效使用至关重要。
.Ss 缓冲区与窗口
.Sy 缓冲区
是文件内容或尚未保存到任何文件的文本在内存中的表示。
.Sy 窗口
是缓冲区的视口。
多个窗口可以同时显示相同或不同的缓冲区。
.Ss 标记环（Mark Ring）
.Sy 标记（mark）
用于定义选中区域的一端（另一端是光标）。
.Nm
维护一个最近标记位置的环形列表。
设置标记时，当前位置会被压入该环中。
.Ss 删除环（Kill Ring）
被删除或复制的文本会存储在
.Sy 删除环
（即剪贴板历史）中。
粘贴（yank）操作可从删除环中检索文本。
连续粘贴可循环遍历之前的删除内容。
.Ss 通用前缀参数
.Cm C-u
命令用于前置数值参数。
默认情况下，
.Cm C-u
将参数设为 4。
重复输入
.Cm C-u
会使参数乘以 4（例如，
.Cm C-u C-u
= 16）。
在
.Cm C-u
后输入数字可将参数设为该数值。
.Sh 命令
命令大体遵循 Emacs 约定。
.Cm C-
表示 Control 键，
.Cm M-
表示 Meta（Alt）键，
.Cm S-
表示 Shift 键。
.Ss 基本命令
.Bl -tag -width Ds
.It Cm C-x C-c
退出编辑器。
.It Cm C-x C-f
打开文件并将其加载到缓冲区。
.It Cm C-x C-s
将当前缓冲区保存到其关联的文件。
.It Cm C-x C-w
将当前缓冲区另存为新文件名（另存为）。
.It Cm C-x C-q
切换当前缓冲区的只读状态。
.It Cm C-x k
关闭（kill）当前缓冲区。
.It Cm C-x b
将当前窗口切换到另一个缓冲区。
.It Cm C-z
挂起编辑器；使用
.Cm fg
恢复。
.It Cm C-x C-z
挂起编辑器并保留编辑器屏幕（Shell Drawer 模式）。
.It Cm C-s
在当前缓冲区中向前交互式搜索。
.It Cm C-r
在当前缓冲区中向后交互式搜索。
.It Cm C-g
取消当前操作或取消选中区域。
.El
.Ss 光标移动
.Bl -tag -width Ds
.It Cm C-n
光标下移一行。
.It Cm C-p
光标上移一行。
.It Cm C-f
光标向前移动一个字符。
.It Cm C-b
光标向后移动一个字符。
.It Cm M-f
光标向前移动一个单词。
.It Cm M-b
光标向后移动一个单词。
.It Cm C-M-f
光标向前移动一个表达式（sexp）。
.It Cm C-M-b
光标向后移动一个表达式（sexp）。
.It Cm M-a
光标向后移动一个句子。
.It Cm M-e
光标向前移动一个句子。
.It Cm M-{
光标向后移动一个段落。
.It Cm M-}
光标向前移动一个段落。
.It Cm M-<
光标移至缓冲区开头。
.It Cm M->
光标移至缓冲区末尾。
.It Cm C-a
光标移至行首。
.It Cm C-e
光标移至行尾。
.It Cm M-v
向上翻页。
.It Cm C-v
向下翻页。
.It Cm M-p
向上滚动一行。
.It Cm M-n
向下滚动一行。
.It Cm C-l
将屏幕中心对齐到光标所在行。
.It Cm M-g
跳转到指定行号。
.It Cm C-x =
显示光标位置（行号、列号、字节偏移）。
.El
.Ss 编辑操作
.Bl -tag -width Ds
.It Cm C-_
撤销。
.It Cm C-x C-_
重做。
.It Cm C-w
剪切（kill）选中区域或矩形区域。
.It Cm M-w
复制选中区域或矩形区域到删除环。
.It Cm C-c
复制选中区域到系统剪贴板（OSC 52）及删除环。
.It Cm C-y
从删除环粘贴（yank）。
.It Cm M-y
粘贴循环：粘贴后循环遍历之前的删除内容。
.It Cm C-k
剪切至行尾。
.It Cm M-d
向前剪切一个单词。
.It Cm M-DEL
向后剪切一个单词。
.It Cm M-z
Zap to char：剪切至指定字符（包含该字符）。
.It Cm C-t
交换相邻字符。
.It Cm M-t
交换相邻单词。
.It Cm C-x C-t
交换相邻句子。
.It Cm C-u
通用前缀：后接数字时，重复执行后续命令指定次数。
.It Cm C-j
插入新行并自动缩进（匹配上一行）。
.It Cm C-o
插入新行但不移动光标。
.It Cm C-q
逐字插入下一个字符（引用插入）。
.It Cm C-x C-l
将选中区域转换为小写。
.It Cm C-x C-u
将选中区域转换为大写。
.It Cm M-c
大写单词首字母。
.It Cm M-u
将单词转为大写。
.It Cm M-l
将单词转为小写。
.It Cm C-h
删除前一个字符（退格键）。
.It Cm DEL
删除光标下的字符。
.El
.Ss 选中与区域
.Sy 区域（region）
定义为
.Sy 标记
与
.Sy 光标
之间的文本。
设置标记后，移动光标会改变活动区域的范围。
.Bl -tag -width Ds
.It Cm C-SPC
设置标记：在光标处建立标记。
.It Cm C-u C-SPC
弹出标记：跳转到上一个标记位置（标记环）。
.It Cm C-x C-x
交换光标与标记的位置。
.It Cm C-g
中止：取消当前选中区域并清除标记。
.It Cm C-x h
标记整个缓冲区：将光标置于缓冲区开头，标记置于末尾。
.El
.Ss 窗口管理
显示区域可划分为多个平铺窗口。
.Bl -tag -width Ds
.It Cm C-x o
切换窗口：将焦点循环切换到下一个可见窗口。
.It Cm C-x 0
关闭窗口：关闭当前聚焦的窗口。
.It Cm C-x 1
关闭其他窗口：将当前窗口扩展至全屏。
.It Cm C-x 2
水平分割窗口：将当前窗口水平拆分为两个。
.It Cm C-x x t
切换自动换行（visual-line-mode）。
.El
.Ss 寄存器（Registers）
寄存器提供会话持久化的光标位置和文本片段存储。每个寄存器由单个字符标识。
.Bl -tag -width Ds
.It Cm C-x r SPACE x
将当前光标位置存储到名为
.Ar x
的寄存器中。
.It Cm C-x r j x
跳转到寄存器
.Ar x
中存储的光标位置。
.It Cm C-x r s x
将活动区域存储到寄存器
.Ar x
中。
.It Cm C-x r y x
粘贴寄存器
.Ar x
的内容。
.It Cm C-x r k x
将区域剪切到寄存器
.Ar x
中。
.It Cm C-x r v x
查看寄存器
.Ar x
的内容。
.El
.Ss 矩形区域（Rectangles）
矩形操作针对跨越多行的列对齐区域。
使用矩形命令前必须先启用矩形标记模式。
.Bl -tag -width Ds
.It Cm C-x SPACE
切换矩形模式。
.It Cm C-x r t 或 C-t
用字符串替换矩形区域。
.It Cm C-x r k 或 C-w
剪切矩形区域。
.It Cm C-x r y 或 C-y
粘贴矩形区域。
.It Cm C-x r o
打开矩形（插入空白）。
.El
.Ss 宏（Macros）
键盘宏用于录制一系列按键操作以便后续回放。
宏无法被查看、编辑、导出或保存。
查找文件、保存文件及 shell 命令无法在宏中执行。
.Bl -tag -width Ds
.It Cm C-x (
开始录制宏。
.It Cm C-x )
停止录制宏。
.It Cm C-x e
执行上一个宏。
.It Cm C-u C-x e
执行上一个宏 N 次。
.El
.Ss 搜索与替换
.Bl -tag -width Ds
.It Cm C-M-s
向前增量正则表达式搜索。
.It Cm C-M-r
向后增量正则表达式搜索。
.It Cm M-%
交互式字符串替换（查询替换）。
.It Cm C-M-%
交互式正则表达式替换（查询替换）。
.It Cm M-x query-replace
交互式替换字符串。
.It Cm M-x replace-string
替换所有匹配项（非交互式）。
.It Cm M-x replace-regexp
使用正则表达式替换。
.El
.Ss Shell 集成
编译时启用时可用：
.Bl -tag -width Ds
.It Cm M-|
通过 shell 命令过滤选中区域。
.It Cm C-u M-|
过滤选中区域并用输出替换原内容。
.It Cm M-!
运行 shell 命令（输出到缓冲区）。
.It Cm M-x diff-buffer-with-file
显示缓冲区与磁盘文件的未保存差异。
.It Cm M-x cd
更改工作目录（仅编辑器内生效）。
.El
.Ss 系统集成
.Bl -tag -width Ds
.It Cm C-c
复制选中区域到系统剪贴板（OSC 52）。
.It Cm M-.
跳转到符号定义处（Ctags）。
.It Cm M-,
从符号定义处跳回。
.It Cm M-x version
显示编辑器版本。
.El
.Ss 命名命令
以下命令可通过
.Cm M-x
调用：
.Bl -tag -width Ds
.It Cm capitalize-region
将区域中每个单词首字母大写。
.It Cm insert-file
在光标处插入另一个文件的内容。
.It Cm cd
更改工作目录。
.It Cm diff-buffer-with-file
显示未保存的更改。
.It Cm isearch-forward-regexp
向前增量正则搜索。
.It Cm query-replace
交互式搜索并替换。
.It Cm replace-regexp
使用正则表达式替换。
.It Cm replace-string
替换字面字符串。
.It Cm revert
从磁盘文件重新加载缓冲区。
.It Cm visual-line-mode
切换自动换行。
.It Cm version
显示版本信息。
.It Cm view-register
显示寄存器内容。
.El
.Sh 与 Emacs 的差异
.Nm
力求兼容 Emacs，但在 shell 集成和可用性方面存在特定差异：
.Bl -tag -width Ds
.It Cm C-h
在 Emacs 中，这是帮助前缀键。在
.Nm
中，它绑定为退格键（Readline 兼容）。
.It Cm C-c
在 Emacs 中，这是用户自定义键绑定前缀。在
.Nm
中，它同时将内容复制到系统剪贴板（OSC 52）和删除环。
.It Cm C-x C-z
在 Emacs 中，这会挂起并清屏。在
.Nm
中，它会挂起但保留编辑器屏幕（Shell Drawer 模式）。
.It Cm C-_
在 Emacs 中，这同时用于撤销和重做，通过反转遍历方向创建撤销历史树。在
.Nm
中，它仅用于撤销。
.El
.Sh 文件与编码
.Nm
期望所有输入和文件均采用
.Sy UTF-8
编码。
文件会整体加载到内存中。
.Pp
.Nm
遵循 Unix 惯例：行以单个换行符（LF）分隔，文件以换行符结尾。
加载文件及插入文本时会验证，不允许空字节（0x00）或格式错误的 UTF-8 序列。
.Ss 文件持久化
保存时，
.Nm
将完整缓冲区写入关联文件。
编辑器会尝试原子写入操作以防止数据损坏。
若无法原子写入，将提示用户直接覆盖文件。
不会生成自动备份或"自动保存"文件。
.Sh 撤销历史
.Nm
为每个缓冲区维护最多 100 次编辑操作的线性撤销历史。
.Sh 另请参阅
.Xr emacs 1
.Sh 问题报告
请将缺陷报告提交至项目仓库：
.Lk https://github.com/nicholascarroll/emil
.Sh 作者
.Nm
编辑器由 Nicholas Carroll 开发。
它是
.Em emsys
（作者：japanoise）的衍生版本。
.Lk
https://github.com/japanoise/emsys
