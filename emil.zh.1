.Dd February 10, 2026
.Dt EMIL 1
.Os
.Sh NAME
.Nm emil
.Nd 文本编辑器
.Sh 概要
.Nm
.Op Ar 选项
.Op Ar 文件
.Sh 描述
.Nm
是小型便携的 UTF-8 文本编辑器。
专为 VT100 兼容终端设计，实现
.Em Emacs
编辑器的操作惯例子集。
.Sh 选项
.Bl -tag -width Ds
.It +N
打开文件时将光标定位到第 N 行。
.It --version
显示版本信息。
.El
.Sh 核心概念
.Nm
遵循 Emacs 范式。
.Ss 缓冲区与窗口
.Sy 缓冲区
是文件或未保存文本在内存中的内容。
.Sy 窗口
是缓冲区的视口。
多个窗口可同时显示相同或不同的缓冲区。
.Ss 标记环（Mark Ring）
.Sy 标记（mark）
定义选中区域的一端（另一端为光标）。
.Nm
维护最近 8 个标记位置的环。
设置标记时当前位置入环，之后可返回这些位置。
.Ss 剪切环（Kill Ring）
被剪切或复制的文本存入
.Sy 剪切环
（剪贴板历史）。
粘贴（yank）从剪切环取出文本插入缓冲区。
连续粘贴可循环遍历之前的剪切内容。
.Ss 通用参数
.Cm C-u
为后续命令附加数值参数。
默认
.Cm C-u
设参数为 4。
重复
.Cm C-u
使参数乘以 4（如
.Cm C-u C-u
= 16）。
在
.Cm C-u
后输入数字可直接设定参数值。
.Sh 命令
命令大体遵循 Emacs 约定。
.Cm C-
表示 Control 键，
.Cm M-
表示 Meta（Alt）键，
.Cm S-
表示 Shift 键。
.Ss 基本命令
.Bl -tag -width Ds
.It Cm C-x C-c
退出编辑器。
.It Cm C-x C-f
打开文件并加载到缓冲区。
.It Cm C-x C-s
保存当前缓冲区到关联文件。
.It Cm C-x C-w
另存为新文件名。
.It Cm C-x C-q
切换当前缓冲区只读状态。
.It Cm C-x k
关闭当前缓冲区。
.It Cm C-x b
切换到另一缓冲区。
.It Cm C-z
挂起编辑器；用
.Cm fg
恢复。
.It Cm C-x C-z
挂起编辑器，保留屏幕（Shell 抽屉）。
.It Cm C-s
在当前缓冲区中向前增量搜索。
.It Cm C-r
在当前缓冲区中向后增量搜索。
.It Cm C-g
取消当前操作或取消选中区域。
.El
.Ss 光标移动
.Bl -tag -width Ds
.It Cm C-n
下移一行。
.It Cm C-p
上移一行。
.It Cm C-f
前移一字符。
.It Cm C-b
后移一字符。
.It Cm M-f
前移一词。
.It Cm M-b
后移一词。
.It Cm C-M-f
前移一表达式（sexp）。
.It Cm C-M-b
后移一表达式（sexp）。
.It Cm M-a
后移一句。
.It Cm M-e
前移一句。
.It Cm M-{
后移一段。
.It Cm M-}
前移一段。
.It Cm M-<
移至缓冲区开头。
.It Cm M->
移至缓冲区末尾。
.It Cm C-a
移至行首。
.It Cm C-e
移至行尾。
.It Cm M-v
上翻一页。
.It Cm C-v
下翻一页。
.It Cm M-p
上滚一行。
.It Cm M-n
下滚一行。
.It Cm C-l
屏幕居中于光标所在行。
.It Cm M-g
跳转至指定行号。
.It Cm C-x =
显示光标位置（行号、列号、字节偏移）。
.El
.Ss 编辑操作
.Bl -tag -width Ds
.It Cm C-_
撤销。
.It Cm C-x C-_
重做。
.It Cm C-w
剪切区域或矩形区域。
.It Cm M-w
复制区域或矩形区域到剪切环。
.It Cm C-c
复制区域到系统剪贴板（OSC 52）及剪切环。
.It Cm C-y
从剪切环粘贴。
.It Cm M-y
粘贴循环：粘贴后遍历之前的剪切内容。
.It Cm C-k
剪切至行尾。
.It Cm M-d
向前剪切一词。
.It Cm M-DEL
向后剪切一词。
.It Cm M-z
Zap to char：剪切至指定字符（含该字符）。
.It Cm C-t
交换相邻字符。
.It Cm M-t
交换相邻单词。
.It Cm C-x C-t
交换相邻句子。
.It Cm C-u
通用参数：后接数字，重复执行后续命令指定次数。
.It Cm C-j
插入新行并缩进（匹配上一行）。
.It Cm C-o
插入新行，光标不动。
.It Cm C-q
逐字插入下一字符（引用插入）。
.It Cm C-x C-l
区域转小写。
.It Cm C-x C-u
区域转大写。
.It Cm M-c
词首字母大写。
.It Cm M-u
整词转大写。
.It Cm M-l
整词转小写。
.It Cm DEL
删除光标下字符。
.It Cm M-/
在弹出缓冲区中打开表情符号及常用 Unicode 符号面板。
移至所需字符后按
.Cm Return
插入。
按
.Cm C-g
取消。
.El
.Ss 选择与区域
.Sy 区域（region）
为
.Sy 标记
与
.Sy 光标
之间的文本。
设置标记后，移动光标改变区域范围。
.Bl -tag -width Ds
.It Cm C-SPC 或 C-@
设置标记：在光标处建立标记。
两者发送相同的控制字节（NUL）。
若输入法占用了
.Cm C-SPC Ns ，
可改用
.Cm C-@ Ns 。
.It Cm C-u C-SPC 或 C-u C-@
弹出标记：跳转至上一标记位置（标记环）。
.It Cm C-x C-x
交换光标与标记位置。
.It Cm C-g
中止：取消当前区域，清除标记。
.It Cm C-x h
标记整个缓冲区：光标置于开头，标记置于末尾。
.It Cm M-h
标记当前段落。
.El
.Ss 窗口管理
显示区域可分割为多个平铺窗口。
.Bl -tag -width Ds
.It Cm C-x o
切换窗口：焦点移至下一可见窗口。
.It Cm C-x 0
关闭窗口：关闭当前窗口。
.It Cm C-x 1
关闭其他窗口：当前窗口扩展至整个屏幕。
.It Cm C-x 2
分割窗口：水平拆分当前窗口。
.It Cm C-x x t
切换自动换行（visual-line-mode）。
.El
.Ss 寄存器（Registers）
寄存器提供会话级的光标位置和文本片段存储。每个寄存器以单个字符标识。
.Bl -tag -width Ds
.It Cm C-x r SPACE x
将当前光标位置存入寄存器
.Ar x Ns 。
.It Cm C-x r j x
跳转至寄存器
.Ar x
中的光标位置。
.It Cm C-x r s x
将区域存入寄存器
.Ar x Ns 。
.It Cm C-x r y x
粘贴寄存器
.Ar x
的内容。
.It Cm C-x r k x
剪切区域至寄存器
.Ar x Ns 。
.It Cm C-x r v x
查看寄存器
.Ar x
的内容。
.El
.Ss 矩形区域（Rectangles）
矩形操作针对跨多行的列对齐区域。
.Bl -tag -width Ds
.It Cm C-x SPACE
切换矩形模式。
.It Cm C-x r t 或 C-t
以字符串替换矩形区域。
.It Cm C-x r k 或 C-w
剪切矩形区域。
.It Cm C-x r y 或 C-y
粘贴矩形区域。
.It Cm C-x r o
打开矩形（插入空白）。
.El
.Ss 宏（Macros）
键盘宏录制按键序列以便回放。
宏不可查看、编辑、导出或保存。
查找文件、保存文件及 shell 命令不可在宏中执行。
.Bl -tag -width Ds
.It Cm C-x (
开始录制。
.It Cm C-x )
停止录制。
.It Cm C-x e
执行上一宏。
.It Cm C-u C-x e
执行上一宏 N 次。
.El
.Ss 搜索与替换
.Bl -tag -width Ds
.It Cm C-M-s
向前增量正则搜索。
.It Cm C-M-r
向后增量正则搜索。
.It Cm M-%
查询替换（字符串）。
.It Cm C-M-%
查询替换（正则表达式）。
.It Cm M-x query-replace
交互式字符串替换。
.It Cm M-x replace-string
替换所有匹配项（非交互式）。
.It Cm M-x replace-regexp
正则表达式替换。
.El
.Ss Shell 集成
编译时启用时可用：
.Bl -tag -width Ds
.It Cm M-|
将选中区域经 shell 命令过滤。
.It Cm C-u M-|
过滤选中区域，以输出替换原内容。
.It Cm M-!
运行 shell 命令（输出至缓冲区）。
.It Cm M-x diff-buffer-with-file
显示相对于磁盘文件的未保存更改。
.It Cm M-x cd
更改工作目录（仅编辑器内生效）。
.El
.Ss 源文件导航
.Bl -tag -width Ds
.It Cm M-.
跳转至符号定义（Ctags）。
.It Cm M-,
从符号定义跳回。
.It Cm M-`
切换头文件与源文件。
.El
.Ss 命名命令
以下命令可通过
.Cm M-x
调用：
.Bl -tag -width Ds
.It Cm capitalize-region
区域中各词首字母大写。
.It Cm insert-file
在光标处插入文件内容。
.It Cm cd
更改工作目录。
.It Cm diff-buffer-with-file
显示未保存的更改。
.It Cm isearch-forward-regexp
向前增量正则搜索。
.It Cm query-replace
交互式搜索替换。
.It Cm replace-regexp
正则表达式替换。
.It Cm replace-string
替换字面量字符串。
.It Cm revert-buffer
从磁盘文件恢复缓冲区。
.It Cm visual-line-mode
切换自动换行。
.It Cm version
显示版本信息。
.It Cm view-register
显示寄存器内容。
.El
.Sh 与 Emacs 的差异
.Nm
力求兼容 Emacs，但使用 POSIX 正则表达式，且以下键绑定有差异：
.Bl -tag -width Ds
.It Cm C-c
在 Emacs 中为用户自定义键绑定前缀。在
.Nm
中同时复制到系统剪贴板（OSC 52）和剪切环。
.It Cm C-x C-z
在 Emacs 中挂起并清屏。在
.Nm
中挂起但保留编辑器屏幕（Shell 抽屉）。
.It Cm C-_
在 Emacs 中兼用于撤销和重做，通过反转遍历方向形成撤销历史树。在
.Nm
中仅用于撤销（撤销/重做为线性操作）。
.El
.Sh 文件与编码
.Nm
要求所有输入和文件采用
.Sy UTF-8
编码。
文件整体加载到内存。
.Pp
.Nm
遵循 Unix 惯例：行以单个换行符（LF）分隔，文件以换行符结尾。
加载文件及向缓冲区插入文本时进行验证，不允许空字节（0x00）或格式错误的 UTF-8 序列。
.Ss 文件持久化
保存时
.Nm
将完整缓冲区写入关联文件。
编辑器尝试原子写入以防数据损坏。
若原子写入不可行，提示用户直接覆盖。
不生成备份或自动保存文件。
.Ss 建议性锁定
文件有未保存的编辑时，
.Nm
对该文件持有建议性锁。若另一
.Nm
实例编辑同一文件，该实例将收到警告。此锁不阻止其他进程写入文件。
.Ss 文件大小限制
.Nm
非为编辑超大文件设计。无法打开超过 1 GB 的文件。
.Sh 另请参阅
.Xr emacs 1
.Sh 缺陷报告
请将缺陷提交至项目仓库：
.Lk https://github.com/nicholascarroll/emil
.Sh 作者
.Nm
编辑器由 Nicholas Carroll 开发，
是 japanoise 所作
.Em emsys
的衍生版本。
.Lk https://github.com/japanoise/emsys
